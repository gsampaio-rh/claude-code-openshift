apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qwen25-14b-model-cache
  namespace: inference
  labels:
    app.kubernetes.io/name: qwen25-14b
    app.kubernetes.io/component: model-serving
    app.kubernetes.io/part-of: agentops-platform
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp3-csi
  resources:
    requests:
      storage: 30Gi
